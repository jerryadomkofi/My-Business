# ByteForge Systems — Landing Page

One-page marketing site for ByteForge Systems, built with plain HTML/CSS/JS (no build step required).

## Run locally
Just open `index.html` in a browser — no server needed.

## Deploy
Works as-is on any static host: GitHub Pages, Netlify, Vercel, etc.
